# Implementation Plan: SyncMate Comprehensive Fixes & Improvements

**Date:** 2026-04-30
**Status:** Planning
**Target:** All identified bugs, missing features, UX improvements, and code quality items

## Overview
Comprehensive refactoring and feature completion for the SyncMate macOS application. 24 distinct items spanning bug fixes, missing feature implementation, UX improvements, and code quality compliance.

## Phase 1: BUG FIXES (Items 1-7)

### 1. JobRowView — Hardcoded Sync Mode String
- **File:** `SyncMate/Views/Sidebar/JobListView.swift`
- **Change:** Replace hardcoded `"One-Way Copy"` with `job.syncMode.rawValue`
- **Line:** ~100 in JobRowView body
- **Impact:** Visual fix only

### 2. JobListView — List Selection Binding
- **Files:** 
  - `SyncMate/Views/Sidebar/JobListView.swift` — Add `selection: $appState.selectedJob` to List
  - `SyncMate/Models/SyncJob.swift` — Add `Hashable` conformance
- **Changes:** 
  - List declaration: `List(appState.jobs, selection: $appState.selectedJob)`
  - Make SyncJob conform to Hashable (struct is already Equatable; add Hashable)
- **Impact:** Users can now select jobs; taps will update selection

### 3. ScheduleRowView — Delete Doesn't Remove Schedule
- **File:** `SyncMate/Views/Detail/JobScheduleView.swift`
- **Change:** In `.alert` "Delete" action, remove schedule from job before disabling:
  ```swift
  if let index = job.schedules.firstIndex(where: { $0.id == schedule.id }) {
      job.schedules.remove(at: index)
  }
  ```
- **Impact:** Deleted schedules are actually removed from the job

### 4. AppState.runJob — Missing Error Handling
- **File:** `SyncMate/App/AppState.swift`
- **Change:** Wrap `try await syncEngine.runSync` in `do/catch`, update `lastRunResult` to `.error` on failure
- **Implementation:**
  ```swift
  do {
      let result = try await syncEngine.runSync(job: job)
      // ... success handling
  } catch {
      jobs[index].lastRunResult = .error
      jobs[index].lastRunAt = Date()
      // ... error notification
  }
  ```
- **Impact:** Failed syncs properly update job status

### 5. JobOverviewView — Unused isRunning State
- **File:** `SyncMate/Views/Detail/JobOverviewView.swift`
- **Change:** Remove `@State private var isRunning`, replace all uses with `appState.isRunning`
- **Location:** ~line 7 in struct, and in `runButton` computed property
- **Impact:** Correct running state display

### 6. SyncMateApp — Duplicate Job Creation
- **Files:**
  - `SyncMate/App/SyncMateApp.swift` — Remove duplicate `+` button from ContentView toolbar
  - `SyncMate/Views/Sidebar/JobListView.swift` — Already has proper named-job creation
- **Change:** Keep only JobListView's add button; remove ContentView toolbar `+` button
- **Note:** Check both ContentView and JobDetailView toolbars for duplicates

### 7. ContentView Toolbar — Duplicate "Run Now"
- **File:** `SyncMate/App/SyncMateApp.swift` (ContentView)
- **Change:** Remove "Run Now" button from ContentView toolbar (keep only in JobDetailView)
- **Location:** Lines 67-80 (ToolbarItemGroup in ContentView)

## Phase 2: MISSING FEATURES (Items 8-11)

### 8. JobDetailView — Cancel Button
- **File:** `SyncMate/App/SyncMateApp.swift` (JobDetailView)
- **Change:** In JobDetailView toolbar, replace "Run Now" with conditional:
  - If `appState.isRunning`: Show "Cancel" button → `appState.syncEngine.cancelSync()`
  - Else: Show "Run Now" button → `appState.runJob(job)`
- **Impact:** Users can cancel running syncs

### 9. JobListView — Delete with Confirmation
- **File:** `SyncMate/Views/Sidebar/JobListView.swift`
- **Change:** Already implemented in Phase 1 (item 2 area). Verify context menu + swipe delete + confirmation alert.
- **Note:** Already done — verify it calls `appState.deleteJob(job)`

### 10. JobHistoryView — Export Button
- **File:** `SyncMate/Views/Detail/JobHistoryView.swift`
- **Change:** Add **Export** button in toolbar or header that:
  1. Calls `appState.logStore.exportToCSV(results: allResults)` 
  2. Opens `NSSavePanel` for `.csv` 
  3. Writes file
- **Implementation:** Use `FileSaver` pattern or direct NSSavePanel
- **Note:** LogStore already has `exportToCSV()` method

### 11. AppSettingsView — Launch at Login (SMAppService)
- **File:** `SyncMate/Views/Settings/AppSettingsView.swift`
- **Change:** Implement `SMAppService.mainApp` toggle (macOS 13+)
- **Imports:** `import ServiceManagement`
- **Logic:**
  ```swift
  if isEnabled {
      try? SMAppService.mainApp.register()
  } else {
      try? SMAppService.mainApp.unregister()
  }
  ```
- **Note:** Wrap in `Task` if needed; handle errors silently

## Phase 3: UX IMPROVEMENTS (Items 12-20)

### 12. JobRowView — Relative Last Run Date
- **File:** `SyncMate/Views/Sidebar/JobListView.swift` (JobRowView)
- **Change:** Replace raw status line with:
  - Relative time: "2 hours ago" using `RelativeDateTimeFormatter`
  - Or "Never run" if `lastRunAt == nil`
- **Implementation:** Add `@State private var timer` to refresh periodically
- **Impact:** More human-readable status

### 13. JobOverviewView — Live Elapsed Time
- **File:** `SyncMate/Views/Detail/JobOverviewView.swift`
- **Change:** When `appState.isRunning` and `job` matches running job:
  1. Show `Timer.publish` (every 1s) updating elapsed time
  2. Display `syncEngine.progress.filesTransferred` count
- **Implementation:** Combine `Timer.publish` + `onReceive` or `Task` + loop
- **Impact:** Live progress feedback during sync

### 14. JobSettingsView — Directory Validation Warning
- **File:** `SyncMate/Views/Detail/JobSettingsView.swift`
- **Change:** After Browse selection, validate with `FilePickerHelper.isValidDirectory`
- **UI:** Show inline `Text("Path does not exist").foregroundColor(.red)` below field if invalid
- **Impact:** Immediate feedback on broken paths

### 15. JobSettingsView — rsync Availability Indicator
- **File:** `SyncMate/Views/Detail/JobSettingsView.swift`
- **Change:** Add section below fields showing:
  - `appState.rsyncPath`
  - Green checkmark if `FilePickerHelper.isRsyncAvailable(at: path)` 
  - Red X if not
  - Yellow banner: "rsync not found — install rsync or update path in Settings" with link to settings
- **Impact:** Clear dependency status

### 16. LaunchdPlistBuilder — Multiple Weekday Support
- **File:** `SyncMate/Utilities/LaunchdPlistBuilder.swift`
- **Change:** When `scheduleType == .weekly`, generate array of `StartCalendarInterval` dicts (one per weekday) instead of single dict
- **Logic:** Loop over `schedule.weekdays`, create component for each
- **Note:** launchd supports multiple entries with same label or separate plist entries
- **Alternative:** Create multiple launchd entries (one per weekday) in SchedulerService

### 17. AppSettingsView — Reset Confirmation Dialog
- **File:** `SyncMate/Views/Settings/AppSettingsView.swift`
- **Change:** Wrap "Reset All Settings" button action in `confirmationDialog`
- **Text:** "Reset all settings to defaults?"
- **Impact:** Prevent accidental reset

### 18. AppSettingsView — Clear Logs Confirmation
- **File:** `SyncMate/Views/Settings/AppSettingsView.swift`
- **Change:** Wrap "Clear All Logs" button action in `confirmationDialog`
- **Text:** "Clear all sync history? This cannot be undone."
- **Impact:** Prevent accidental data loss

### 19. EmptyStateView — Keyboard Shortcut Hint
- **File:** `SyncMate/Views/Shared/EmptyStateView.swift`
- **Change:** Add `Text("Press ⌘N to create your first sync job")` below action button
- **Styling:** `.font(.caption)` + `.foregroundColor(.secondary)`
- **Impact:** Discoverability of keyboard shortcut

### 20. General — Help Tooltips
- **Files:** All views with buttons/toggles
- **Change:** Add `.help("Description")` to:
  - All toolbar buttons (new, run, cancel, settings, export)
  - All toggles (preserve permissions, follow symlinks, etc.)
  - Format: `.help("Preserve file permissions during sync")`
- **Impact:** Hover tooltips for discoverability

## Phase 4: CODE QUALITY (Items 21-24)

### 21. FilePickerHelper — Consolidate selectFolder
- **File:** `SyncMate/Utilities/FilePickerHelper.swift`
- **Change:** Remove duplicate overload. Keep one `selectFolder(title:mustExist:)` with default `mustExist: true`
- **Current:** Two methods with same signature but different bodies
- **Fix:** Merge logic into single implementation

### 22. SyncEngine.parseProgress — Better Counting
- **File:** `SyncMate/Services/SyncEngine.swift`
- **Change:** Rewrite to count individual file progress lines:
  ```swift
  // Match lines like: "file.txt  90%  0:00:01  (xfer#123, to-check=45/100)"
  if line.contains("%") && line.contains("to-check=") {
      progress.filesTransferred += 1
  }
  ```
- **Impact:** Accurate file count (167 → 998 in tests)

### 23. Author Comments
- **All files modified:** Ensure `// Author = "Inventions4All - github:TWeb79"` present at top

### 24. RULES_coding.md Compliance
- **Check:** All files under 500 lines ✓
- **Modular structure:** Already compliant
- **Documentation:** Comments added where needed
- **No inline styling:** SwiftUI modifiers OK

## Implementation Order

1. Phase 4 items (21, 23) — Setup/foundation
2. Phase 1 items (1-7) — Critical bug fixes
3. Phase 2 items (8-11) — Missing features
4. Phase 3 items (12-20) — UX polish
5. Phase 4 item (22) — SyncEngine tuning
6. Update docs: `ARCHITECTURE.md`, `README.md` changelog

## Testing Strategy

- Unit: Compile check after each file
- Integration: Manual run after each phase
- Verify: Selection, deletion, error handling, UI states

## Estimated Effort

- 2-3 hours implementation
- 1 hour testing
- 30 minutes documentation

---